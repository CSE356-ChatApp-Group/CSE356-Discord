name: Reusable VM Deploy

on:
  workflow_call:
    inputs:
      environment:
        description: Deployment environment name
        required: true
        type: string
      target_sha:
        description: Commit SHA to deploy
        required: true
        type: string
      host:
        description: Remote host
        required: true
        type: string
      user:
        description: Remote SSH user
        required: true
        type: string
      script_path:
        description: Deploy script to execute
        required: true
        type: string
      artifact_name:
        description: Current-run artifact name to deploy instead of polling GitHub Releases
        required: false
        default: ''
        type: string
      verify_public_host:
        description: Verify public HTTP endpoints after deploy
        required: false
        default: false
        type: boolean
      auto_confirm:
        description: Pipe yes into interactive deploy scripts
        required: false
        default: false
        type: boolean
      chatapp_instances:
        description: Node HTTP worker count (deploy-prod CHATAPP_INSTANCES); use 2 when nginx load-balances chatapp@4000 and @4001
        required: false
        default: ''
        type: string
    secrets:
      DEPLOY_SSH_KEY:
        required: true
      SSH_KNOWN_HOSTS:
        required: false

permissions:
  contents: read
  actions: read

env:
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: "true"

jobs:
  deploy:
    runs-on: ubuntu-latest
    timeout-minutes: 25
    environment: ${{ inputs.environment }}
    steps:
      - name: Checkout
        uses: actions/checkout@v5

      - name: Ensure GitHub CLI exists
        if: ${{ inputs.artifact_name == '' }}
        shell: bash
        run: |
          if ! command -v gh >/dev/null 2>&1; then
            sudo apt-get update
            sudo apt-get install -y gh
          fi

      - name: Wait for release artifact
        if: ${{ inputs.artifact_name == '' }}
        shell: bash
        env:
          GH_TOKEN: ${{ github.token }}
          TARGET_SHA: ${{ inputs.target_sha }}
        run: |
          set -euo pipefail
          TAG="release-$TARGET_SHA"
          MAX_ATTEMPTS=40
          SLEEP_SECONDS=15

          echo "Waiting for release artifact: $TAG"
          for attempt in $(seq 1 "$MAX_ATTEMPTS"); do
            if gh release view "$TAG" -R "${{ github.repository }}" >/dev/null 2>&1; then
              echo "Release artifact is ready."
              exit 0
            fi
            echo "Attempt $attempt/$MAX_ATTEMPTS: release not ready yet; sleeping ${SLEEP_SECONDS}s"
            sleep "$SLEEP_SECONDS"
          done

          echo "ERROR: release artifact $TAG not available after waiting."
          exit 1

      - name: Download current-run artifact
        if: ${{ inputs.artifact_name != '' }}
        uses: actions/download-artifact@v8
        with:
          name: ${{ inputs.artifact_name }}
          path: .artifacts

      - name: Resolve local artifact path
        id: artifact
        if: ${{ inputs.artifact_name != '' }}
        shell: bash
        run: |
          set -euo pipefail
          FILE=$(find .artifacts -type f -name '*.tar.gz' | head -n 1)
          test -n "$FILE" || { echo "Artifact tarball not found after download"; exit 1; }
          echo "local_path=$FILE" >> "$GITHUB_OUTPUT"

      - name: Validate deploy inputs
        shell: bash
        run: |
          test -n "${{ inputs.host }}" || { echo "Missing deployment host"; exit 1; }
          test -n "${{ inputs.user }}" || { echo "Missing deployment user"; exit 1; }

      - name: Setup SSH key
        shell: bash
        env:
          DEPLOY_SSH_KEY: ${{ secrets.DEPLOY_SSH_KEY }}
        run: |
          test -n "$DEPLOY_SSH_KEY" || { echo "Missing DEPLOY_SSH_KEY secret"; exit 1; }
          mkdir -p ~/.ssh
          chmod 700 ~/.ssh
          printf '%s\n' "$DEPLOY_SSH_KEY" > ~/.ssh/id_ed25519
          chmod 600 ~/.ssh/id_ed25519
          # Multiplex all SSH connections over one TCP session to avoid
          # triggering fail2ban with rapid successive connections.
          touch ~/.ssh/config
          chmod 600 ~/.ssh/config
          printf 'Host *\n  ControlMaster auto\n  ControlPath ~/.ssh/ctl-%%h-%%p-%%r\n  ControlPersist 5m\n' >> ~/.ssh/config

      - name: Add host to known_hosts
        shell: bash
        env:
          SSH_KNOWN_HOSTS: ${{ secrets.SSH_KNOWN_HOSTS }}
        run: |
          touch ~/.ssh/known_hosts
          if [[ -n "$SSH_KNOWN_HOSTS" ]]; then
            printf '%s\n' "$SSH_KNOWN_HOSTS" >> ~/.ssh/known_hosts
          else
            for attempt in 1 2 3; do
              ssh-keyscan -T 15 -H "${{ inputs.host }}" >> ~/.ssh/known_hosts && break
              echo "keyscan attempt ${attempt}/3 failed; retrying in 5s"
              sleep 5
            done
            test -s ~/.ssh/known_hosts || { echo "Failed to populate known_hosts"; exit 1; }
          fi
          chmod 600 ~/.ssh/known_hosts

      - name: Deploy
        shell: bash
        env:
          GH_TOKEN: ${{ github.token }}
          TARGET_SHA: ${{ inputs.target_sha }}
          DEPLOY_HOST: ${{ inputs.host }}
          DEPLOY_USER: ${{ inputs.user }}
          LOCAL_ARTIFACT_PATH: ${{ steps.artifact.outputs.local_path }}
          CHATAPP_INSTANCES_INPUT: ${{ inputs.chatapp_instances }}
        run: |
          set -euo pipefail
          EXTRA_ENV=()
          if [[ -n "${CHATAPP_INSTANCES_INPUT}" ]]; then
            EXTRA_ENV+=("CHATAPP_INSTANCES=${CHATAPP_INSTANCES_INPUT}")
          fi
          if [[ "${{ inputs.auto_confirm }}" == "true" ]]; then
            printf 'y\n' | env "${EXTRA_ENV[@]}" LOCAL_ARTIFACT_PATH="$LOCAL_ARTIFACT_PATH" STAGING_HOST="$DEPLOY_HOST" STAGING_USER="$DEPLOY_USER" PROD_HOST="$DEPLOY_HOST" PROD_USER="$DEPLOY_USER" GITHUB_REPO="${{ github.repository }}" \
              "./${{ inputs.script_path }}" "$TARGET_SHA"
          else
            env "${EXTRA_ENV[@]}" LOCAL_ARTIFACT_PATH="$LOCAL_ARTIFACT_PATH" STAGING_HOST="$DEPLOY_HOST" STAGING_USER="$DEPLOY_USER" PROD_HOST="$DEPLOY_HOST" PROD_USER="$DEPLOY_USER" GITHUB_REPO="${{ github.repository }}" \
              "./${{ inputs.script_path }}" "$TARGET_SHA"
          fi

      - name: Verify public endpoints
        if: ${{ inputs.verify_public_host }}
        shell: bash
        run: |
          curl -fsS --max-time 10 "http://${{ inputs.host }}/health" >/dev/null
          curl -fsS --max-time 10 "http://${{ inputs.host }}/" >/dev/null
          echo "Public endpoint checks passed for ${{ inputs.host }}"
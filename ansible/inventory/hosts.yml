# Hosts match docs/infrastructure-inventory.md — override ansible_host/ansible_user locally if IPs change.
---
all:
  children:
    staging:
      hosts:
        staging-vm:
          ansible_host: 136.114.103.71
          ansible_user: ssperrottet
    production:
      hosts:
        prod-app:
          ansible_host: 130.245.136.44
          ansible_user: ubuntu

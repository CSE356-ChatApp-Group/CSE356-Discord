meta {
  name: Set My Presence
  type: http
  seq: 2
}

put {
  url: {{baseUrl}}/presence
  body: json
  auth: bearer
}

auth:bearer {
  token: {{accessToken}}
}

body:json {
  {
    "status": "online"
  }
}

docs {
  Valid statuses: online | idle | away | offline
}

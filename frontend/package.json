{
  "name": "frontend",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "workspaces": [
    "."
  ],
  "main": "src/main.tsx",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "typecheck": "tsc --noEmit",
    "preview": "vite preview",
    "test": "vitest run --config vite.config.test.ts",
    "e2e:install": "npx playwright install chromium",
    "e2e": "vite build && npx playwright test --grep @smoke",
    "e2e:ci": "vite build && npx playwright test --grep @smoke --reporter=list",
    "e2e:full": "vite build && npx playwright test --grep @staging --workers=1 --retries=2",
    "e2e:full:ci": "vite build && npx playwright test --grep @staging --workers=1 --retries=2 --reporter=list",
    "e2e:quick": "vite build && npx playwright test --grep @full --grep-invert @heavy-auth",
    "e2e:quick:ci": "vite build && npx playwright test --grep @full --grep-invert @heavy-auth --reporter=list",
    "e2e:staging": "vite build && npx playwright test --grep @staging --workers=1 --retries=0",
    "e2e:staging:ci": "vite build && npx playwright test --grep @staging --workers=1 --retries=2",
    "e2e:heavy": "vite build && npx playwright test --grep @heavy-auth",
    "e2e:experimental": "vite build && npx playwright test --grep @experimental --workers=1 --retries=0",
    "e2e:auth": "vite build && npx playwright test e2e/auth.spec.ts",
    "e2e:messaging": "vite build && npx playwright test e2e/messaging.spec.ts",
    "e2e:community": "vite build && npx playwright test e2e/community.spec.ts",
    "e2e:dm-invite": "vite build && npx playwright test e2e/dm-invite-multi-client.spec.ts",
    "e2e:dm-first-open": "vite build && npx playwright test e2e/dm-first-open-after-send.spec.ts",
    "e2e:dm-realtime": "vite build && npx playwright test e2e/dm-realtime-delivery.spec.ts",
    "e2e:headed": "vite build && npx playwright test --headed",
    "e2e:ui": "vite build && npx playwright test --ui"
  },
  "devDependencies": {
    "@playwright/test": "^1.54.2",
    "@testing-library/jest-dom": "^6.8.0",
    "@testing-library/react": "^16.1.0",
    "@types/react": "^18.3.3",
    "@types/react-dom": "^18.3.0",
    "@vitejs/plugin-react": "^4.7.0",
    "jsdom": "^25.0.1",
    "typescript": "^5.7.3",
    "vite": "^7.3.1",
    "vitest": "^4.1.2"
  },
  "dependencies": {
    "@tanstack/react-virtual": "^3.13.23",
    "date-fns": "^3.6.0",
    "react": "^18.3.1",
    "react-dom": "^18.3.1",
    "react-router-dom": "^6.24.1",
    "zustand": "^4.5.4"
  }
}

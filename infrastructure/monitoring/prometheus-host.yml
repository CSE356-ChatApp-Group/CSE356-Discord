global:
  scrape_interval: 15s
  evaluation_interval: 15s
  external_labels:
    cluster: chatapp-__ALERT_ENVIRONMENT__
    environment: __ALERT_ENVIRONMENT__

alerting:
  alertmanagers:
    - static_configs:
        - targets: ['127.0.0.1:9093']

rule_files:
  - /etc/prometheus/alerts.yml

scrape_configs:
  - job_name: 'chatapp-api'
    metrics_path: '/metrics'
    static_configs:
      # Updated by deploy script (step 10.6) to the active port after each cutover.
      - targets: ['127.0.0.1:4001']
        labels:
          node: live

  - job_name: 'prometheus'
    static_configs:
      - targets: ['127.0.0.1:9090']
